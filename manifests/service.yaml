apiVersion: v1
kind: Service
metadata:
  name: sre-demo-api
  namespace: controlled-release-demo
  labels:
    app: sre-demo-api
spec:
  type: ClusterIP
  selector:
    app: sre-demo-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
